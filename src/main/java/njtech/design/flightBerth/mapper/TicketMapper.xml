<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="njtech.design.flightBerth.dao.TicketMapper">
    <resultMap id="RM" type="njtech.design.flightBerth.entity.Ticket">
        <id property="id" column="id" jdbcType="INTEGER"/>
        <result property="ticCode" column="tic_code" jdbcType="VARCHAR"/>
        <result property="identity" column="identity" jdbcType="VARCHAR"/>
        <result property="userName" column="user_name" jdbcType="VARCHAR"/>
        <result property="buyWay" column="buy_way" jdbcType="INTEGER"/>
        <result property="airCompanyName" column="air_company_name" jdbcType="VARCHAR"/>
        <result property="flightNum" column="flight_num" jdbcType="VARCHAR"/>
        <result property="start" column="start" jdbcType="VARCHAR"/>
        <result property="destination" column="destination" jdbcType="VARCHAR"/>
        <result property="flightTime" column="flight_time" jdbcType="TIMESTAMP"/>
        <result property="flightDate" column="flight_date" jdbcType="DATE"/>
        <result property="berthName" column="berth_name" jdbcType="VARCHAR"/>
        <result property="gateNum" column="gate_num" jdbcType="VARCHAR"/>
        <result property="seatNum" column="seat_num" jdbcType="VARCHAR"/>
        <result property="remark" column="remark" jdbcType="VARCHAR"/>
    </resultMap>

    <select id="getFlightAndTicket" resultMap="RM">
        select * from ticket where identity = #{identity} and flight_num = #{flightNum} and flight_time=#{flightDate}
        <if test="startPlace!=null and startPlace!=''">
            and start = #{startPlace}
        </if>
        <if test="targetPlace!=null and targetPlace!=''">
            and destination = #{targetPlace}
        </if>
        and remark = #{remark}
    </select>

    <insert id="insertTicket" parameterType="njtech.design.flightBerth.entity.Ticket">
        INSERT INTO
        ticket
        (tic_code,identity,user_name,buy_way,flight_num,start,destination,flight_time,flight_date,gate_num,seat_num,remark,air_company_name,berth_name)
        VALUES
        (#{ticCode},#{identity},#{userName},#{buyWay},#{flightNum},#{start},#{destination},#{flightTime},#{flightDate},#{gateNum},#{seatNum},#{remark},#{airCompanyName},#{berthName})
    </insert>

    <update id="updateTicketRemarkById">
        UPDATE
        ticket
        SET
        remark=#{remark}
        WHERE
        id=#{id}
    </update>
    <!--public List<Ticket> selectAll();-->
    <!--<select id="selectAll" resultMap="RM">-->
    <!--SELECT-->
    <!--b.id id,-->
    <!--b.tic_code tic_code,-->
    <!--b.identity identity,-->
    <!--b.user_name user_name,-->
    <!--b.buy_way buy_way,-->
    <!--b.flight_num flight_num,-->
    <!--b.start start,-->
    <!--b.destination destination,-->
    <!--b.flight_time flight_time,-->
    <!--b.flight_date flight_date,-->
    <!--b.gate_num gate_num,-->
    <!--b.seat_num seat_num,-->
    <!--b.remark remark,-->
    <!--(-->
    <!--SELECT-->
    <!--c.air_company_name-->
    <!--FROM aircompany_info c-->
    <!--WHERE c.air_company_code=b.air_company_code-->
    <!--)AS air_company_name-->
    <!--(-->
    <!--SELECT-->
    <!--d.berth_name-->
    <!--FROM berth_info-->
    <!--WHERE d.berth_code=b.berth_code-->
    <!--)AS berth_name-->
    <!--FROM tic_info b-->
    <!--</select>-->
    <!--&lt;!&ndash; public Ticket selectByTicCode(int ticCode);&ndash;&gt;-->
    <!--<select id="selectByTicCode" resultMap="RM">-->
    <!--SELECT-->
    <!--b.id id,-->
    <!--b.tic_code tic_code,-->
    <!--b.identity identity,-->
    <!--b.user_name user_name,-->
    <!--b.buy_way buy_way,-->
    <!--b.flight_num flight_num,-->
    <!--b.start start,-->
    <!--b.destination destination,-->
    <!--b.flight_time flight_time,-->
    <!--b.flight_date flight_date,-->
    <!--b.gate_num gate_num,-->
    <!--b.seat_num seat_num,-->
    <!--b.remark remark,-->
    <!--(-->
    <!--SELECT-->
    <!--c.air_company_name-->
    <!--FROM aircompany_info c-->
    <!--WHERE c.air_company_code=b.air_company_code-->
    <!--)AS air_company_name-->
    <!--(-->
    <!--SELECT-->
    <!--d.berth_name-->
    <!--FROM berth_info-->
    <!--WHERE d.berth_code=b.berth_code-->
    <!--)AS berth_name-->
    <!--FROM tic_info b-->
    <!--WHERE b.tic_code=#{ticCode}-->
    <!--</select>-->
    <!--&lt;!&ndash;public List<Ticket> selectByIdentity(int identity);&ndash;&gt;-->
    <!--<select id="selectByIdentity" resultMap="RM">-->
    <!--SELECT-->
    <!--b.id id,-->
    <!--b.tic_code tic_code,-->
    <!--b.identity identity,-->
    <!--b.user_name user_name,-->
    <!--b.buy_way buy_way,-->
    <!--b.flight_num flight_num,-->
    <!--b.start start,-->
    <!--b.destination destination,-->
    <!--b.flight_time flight_time,-->
    <!--b.flight_date flight_date,-->
    <!--b.gate_num gate_num,-->
    <!--b.seat_num seat_num,-->
    <!--b.remark remark,-->
    <!--(-->
    <!--SELECT-->
    <!--c.air_company_name-->
    <!--FROM aircompany_info c-->
    <!--WHERE c.air_company_code=b.air_company_code-->
    <!--)AS air_company_name-->
    <!--(-->
    <!--SELECT-->
    <!--d.berth_name-->
    <!--FROM berth_info-->
    <!--WHERE d.berth_code=b.berth_code-->
    <!--)AS berth_name-->
    <!--FROM tic_info b-->
    <!--WHERE b.identity=#{identity}-->
    <!--</select>-->
    <!--&lt;!&ndash;public int insertTicket(Ticket ticket);&ndash;&gt;-->
    <!--<insert id="insertTicket" parameterType="ticket">-->
    <!--INSERT INTO-->
    <!--tic_info-->
    <!--(tic_code,identity,user_name,buy_way,flight_num,start,destination,flight_time,flight_date,gate_num,gate_seat,remark,air_company_code,berth_code)-->
    <!--VALUES-->
    <!--(#{ticCode},#{userInfo.identity},#{userInfo.name},#{buyWay},#{flightNum},#{start},#{destination},#{flightTime},#{flightDate},#{gateNum},#{gateSeat},#{remark},#{airCompany.airCompanyCode},#{berth.berthCode})-->
    <!--</insert>-->
    <!--&lt;!&ndash;public int updateTicket(Ticket ticket);&ndash;&gt;-->
    <!--<update id="updateTicket" parameterType="ticket">-->
    <!--UPDATE-->
    <!--tic_info-->
    <!--SET-->
    <!--tic_code=#{ticCode}-->
    <!--identity=#{userInfo.identity}-->
    <!--user_name=#{userInfo.name}-->
    <!--buy_way=#{buyWay}-->
    <!--flight_num=#{flightNum}-->
    <!--start=#{start}-->
    <!--destination=#{destination}-->
    <!--flight_time=#{flightTime}-->
    <!--flight_date=#{flightDate}-->
    <!--gate_num=#{gateNum}-->
    <!--gate_seat=#{gateSeat}-->
    <!--reamrk=#{remark}-->
    <!--air_company_code=#{airCompany.airCompanyCode}-->
    <!--berth_code=#{berth.berthCode}-->
    <!--WHERE-->
    <!--tic_code=#{ticCode}-->
    <!--</update>-->
    <!--&lt;!&ndash;public int deleteTicket(Ticket ticket);&ndash;&gt;-->
    <!--<delete id="deleteTicket" parameterType="ticket">-->
    <!--DELETE FROM-->
    <!--tic_info-->
    <!--WHERE-->
    <!--tic_code=#{ticCode}-->
    <!--</delete>-->
</mapper>